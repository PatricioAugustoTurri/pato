# Lo que nunca tiene que entrar a la imagen.
# `.next` local pesa 1,8 GB y `node_modules` 601 MB: sin esta lista, cada build
# empieza mandandole eso al motor de Docker antes de hacer nada.
node_modules
.next
.git
.env*
npm-debug.log*
*.tsbuildinfo
.DS_Store
.claude
.impeccable
